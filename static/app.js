const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const state={user:null,displayName:null,role:null,demo:true,page:'dashboard',case:null,auditId:null,scores:{},annotations:[],audits:[],files:[]};
const OPD=[
 ['ข้อมูลผู้ป่วย (Patient profile)',['ชื่อ–สกุล เพศ HN อายุหรือวันเกิดครบถ้วน','ที่อยู่ปัจจุบันและเลขบัตรประชาชน','ญาติ/ผู้ติดต่อฉุกเฉิน ความสัมพันธ์ และเบอร์โทร','ประวัติแพ้ยา/แพ้สิ่งอื่น หรือระบุไม่ทราบ','หมู่เลือด หรือระบุไม่ทราบ','วันที่บันทึกและชื่อผู้รับผิดชอบ','ชื่อ–สกุลและ HN ปรากฏทุกหน้า']],
 ['ประวัติการเจ็บป่วย (History)',['Chief complaint ระบุอาการสำคัญ','Present illness และการรักษาที่ได้รับก่อนมา','โรคประจำตัวและการรักษาปัจจุบัน','อดีตเจ็บป่วยและประวัติครอบครัว','ประวัติแพ้ยา','ประวัติเฉพาะวัย/เพศ หรือ Social history','สารเสพติด บุหรี่ สุรา พร้อมจำนวน ความถี่ ระยะเวลา']],
 ['การตรวจร่างกาย (Physical examination)',['วันและเวลาที่ตรวจครั้งแรก','การตรวจดู/เคาะสอดคล้องกับอาการ','การตรวจคลำ/ฟังสอดคล้องกับอาการ','Pulse, respiration, temperature ครบ','Blood pressure (ยกเว้นเด็กอายุต่ำกว่า 3 ปี)','น้ำหนักและส่วนสูงตามข้อบ่งชี้','สรุปวินิจฉัยสอดคล้องกับประวัติและการตรวจ']],
 ['การรักษา/ตรวจวินิจฉัย',['คำสั่งและผล Lab/รังสี/การตรวจอื่น','การรักษา ยา หัตถการสอดคล้องกับวินิจฉัย','ชื่อยา ความแรง ขนาด และจำนวนครบ','มีคำแนะนำเรื่องโรคและการปฏิบัติตัว','มีผล Consult เมื่อส่งปรึกษา','แผนรักษาต่อเนื่อง/นัดติดตาม','ลายมือชื่อแพทย์ผู้รับผิดชอบ']],
 ['การตรวจติดตาม (Follow up)',['ประวัติ/เหตุผลที่มาติดตาม','วินิจฉัยสอดคล้องกับการรักษา','Vital signs และตรวจร่างกายที่จำเป็น','ประเมินผลครั้งก่อนและการรักษาครั้งนี้','ผล Lab/รังสีที่เกี่ยวข้อง','คำแนะนำและแผนติดตามครั้งต่อไป','ลายมือชื่อแพทย์ผู้รับผิดชอบ']],
 ['บันทึกหัตถการ (Operative note)',['ระบุตัวผู้ป่วยชัดเจน','Operative findings','Operative procedures','วิธีให้ยาชาหรือระงับความรู้สึก','ผล/วินิจฉัยหลังทำ/ภาวะแทรกซ้อน','วันและเวลาเริ่ม–สิ้นสุด','ลายมือชื่อผู้ทำหัตถการ']],
 ['หนังสือยินยอม (Informed consent)',['ระบุตัวผู้ป่วยถูกต้อง','ลายมือชื่อผู้รับทราบ/ผู้ปกครอง','ลายมือชื่อพยานครบ','เหตุผลและความจำเป็น','ข้อมูลภาวะแทรกซ้อน','ลายมือชื่อผู้ให้ข้อมูล','วันและเวลาที่รับทราบ']]
];
const IPD_NAMES=['Discharge summary: Diagnosis / Operation','Discharge summary: Others','Informed consent','History','Physical examination','Progress note & Doctor’s order','Nurses’ note','Consultation record','Anesthetic record','Operative note','Labour record','Rehabilitation record'];
const IPD=IPD_NAMES.map((name,idx)=>[name,[
 'ระบุตัวผู้ป่วย HN/AN ถูกต้องทุกหน้า','วันและเวลาบันทึกชัดเจน','ข้อมูลสำคัญครบตามบริบทการรักษา','เนื้อหาสอดคล้องกับอาการและการวินิจฉัย','มีการประเมินและแผนการรักษาต่อเนื่อง','บันทึกผลการรักษา/ผลตรวจที่เกี่ยวข้อง','ใช้คำศัพท์และรหัสมาตรฐาน ไม่ใช้ตัวย่อคลุมเครือ','ลงชื่อและระบุผู้รับผิดชอบตรวจสอบได้','บันทึกมีความต่อเนื่อง อ่านได้ และไม่ขัดแย้งกัน'
].map((x,i)=> idx===0&&i===2?'Final diagnosis, ICD-10, operation และ clinical summary ครบ':x)]);

async function api(path,options={}){const r=await fetch(path,{headers:options.body instanceof FormData?{}:{'Content-Type':'application/json'},...options});if(r.status===401){showLogin();throw new Error('กรุณาเข้าสู่ระบบ')}let data;try{data=await r.json()}catch{data={detail:'เกิดข้อผิดพลาด'}}if(!r.ok)throw new Error(data.detail||'เกิดข้อผิดพลาด');return data}
function toast(msg){const el=$('#toast');el.textContent=msg;el.classList.add('show');clearTimeout(el.t);el.t=setTimeout(()=>el.classList.remove('show'),2600)}
function showLogin(){ $('#login').classList.remove('hidden');$('#app').classList.add('hidden');$('#loginForm').classList.remove('hidden');$('#registerForm').classList.add('hidden') }
function showApp(){ $('#login').classList.add('hidden');$('#app').classList.remove('hidden') }
function fmtDate(d){if(!d)return'-';return new Intl.DateTimeFormat('th-TH',{day:'numeric',month:'short',year:'2-digit'}).format(new Date(d))}
function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function badge(s){let c=s.includes('เสร็จ')||s.includes('ผ่าน')?'green':s.includes('ไม่ครบ')||s.includes('ทบทวน')?'red':'amber';return `<span class="badge ${c}">● ${esc(s)}</span>`}

function applyUser(u){state.user=u.user;state.displayName=u.display_name||u.user;state.role=u.role;$('#currentUser').textContent=state.displayName;const admin=u.role==='admin';$('#memberNav').classList.toggle('hidden',!admin);$('#activityNav').classList.toggle('hidden',!admin);$('#newAuditBtn').classList.toggle('hidden',u.role==='viewer')}
async function boot(){const s=await api('/api/session');state.demo=s.demo;$('#demoHint').style.display=s.demo?'block':'none';if(!s.authenticated)return showLogin();applyUser(s);showApp();$('#thaiDate').textContent=new Intl.DateTimeFormat('th-TH',{dateStyle:'long'}).format(new Date());health();navigate('dashboard')}
$('#loginForm').onsubmit=async e=>{e.preventDefault();try{const d=await api('/api/login',{method:'POST',body:JSON.stringify({username:$('#username').value,password:$('#password').value})});applyUser(d);$('#thaiDate').textContent=new Intl.DateTimeFormat('th-TH',{dateStyle:'long'}).format(new Date());$('#loginError').textContent='';showApp();navigate('dashboard');health()}catch(err){$('#loginError').textContent=err.message}}
$('#showRegister').onclick=()=>{$('#loginForm').classList.add('hidden');$('#registerForm').classList.remove('hidden');$('#registerError').textContent=''};
$('#backLogin').onclick=()=>showLogin();
$('#registerForm').onsubmit=async e=>{e.preventDefault();const error=$('#registerError');error.textContent='';try{const d=await api('/api/register',{method:'POST',body:JSON.stringify({display_name:$('#regDisplayName').value,department:$('#regDepartment').value,username:$('#regUsername').value,password:$('#regPassword').value,password_confirm:$('#regConfirm').value})});toast(d.message);e.target.reset();showLogin();$('#loginError').textContent='ส่งคำขอแล้ว กรุณารอผู้ดูแลระบบอนุมัติ'}catch(err){error.textContent=err.message}};
$('#logout').onclick=async()=>{await api('/api/logout',{method:'POST'});showLogin()};
async function health(){try{const h=await api('/api/health');const label={connected:'เชื่อมต่อ HOSxP แล้ว',demo:'โหมดข้อมูลสาธิต',not_configured:'รอตั้งค่าฐานข้อมูล',error:'เชื่อมต่อไม่สำเร็จ'}[h.hosxp];$('#connectionText').textContent=label;$('.connection i').classList.toggle('ok',['connected','demo'].includes(h.hosxp))}catch{}}
$$('.nav-item').forEach(b=>b.onclick=()=>navigate(b.dataset.page));$('#newAuditBtn').onclick=()=>navigate('records');$('#menuBtn').onclick=()=>$('.sidebar').classList.toggle('open');
const titles={dashboard:['หน้าหลัก / ภาพรวม','ภาพรวมคุณภาพเวชระเบียน'],records:['งานตรวจประเมิน / ค้นหา','ค้นเวชระเบียน'],audits:['งานตรวจประเมิน / รายการ','รายการประเมิน'],reports:['รายงาน / คุณภาพ','รายงานคุณภาพ'],members:['ระบบ / สมาชิก','สมาชิกและสิทธิ์การใช้งาน'],activity:['ระบบ / Audit trail','ประวัติการใช้งาน'],workspace:['งานตรวจประเมิน / แบบประเมิน','พื้นที่ตรวจประเมิน']};
async function navigate(page){if((page==='members'||page==='activity')&&state.role!=='admin')return toast('บัญชีนี้ไม่มีสิทธิ์เข้าถึง');state.page=page;$$('.page').forEach(x=>x.classList.remove('active'));const el=$(`#page-${page}`);el.classList.add('active');$$('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.page===page));$('#breadcrumb').textContent=titles[page][0];$('#pageTitle').textContent=titles[page][1];el.classList.toggle('workspace-page',page==='workspace');$('.sidebar').classList.remove('open');if(page==='dashboard')await dashboard();if(page==='records')records();if(page==='audits')await audits();if(page==='reports')await reports();if(page==='members')await members();if(page==='activity')await activity()}

async function dashboard(){const d=await api('/api/dashboard'),a=await api('/api/audits?limit=5');const cards=[['แบบประเมินทั้งหมด',d.today,'▦'],['ประเมินเสร็จสิ้น',d.completed,'✓'],['กำลังดำเนินการ',d.pending,'◷'],['คะแนนคุณภาพเฉลี่ย',`${d.average}%`,'↗']];$('#page-dashboard').innerHTML=`<div class="hero"><div><p class="eyebrow">QUALITY COMMAND CENTER</p><h1>สวัสดีครับ ทีมเวชระเบียน</h1><p>ติดตามงานตรวจประเมินและประเด็นคุณภาพจากจุดเดียว</p></div><span class="badge green">● ระบบพร้อมใช้งาน</span></div><div class="stats">${cards.map(x=>`<div class="stat"><div class="stat-top"><small>${x[0]}</small><span class="stat-icon">${x[2]}</span></div><strong>${x[1]}</strong><span class="delta">ข้อมูลล่าสุดในระบบ QMR</span></div>`).join('')}</div><div class="grid-main"><div class="card"><div class="card-head"><h3>การประเมินล่าสุด</h3><button class="link-btn" onclick="navigate('audits')">ดูทั้งหมด →</button></div>${a.items.length?a.items.map(recent).join(''):'<div class="empty">ยังไม่มีรายการประเมิน<br><button class="primary" style="margin-top:15px" onclick="navigate(\'records\')">เริ่มรายการแรก</button></div>'}</div><div class="card"><div class="card-head"><h3>ภาพรวมเกณฑ์สำคัญ</h3><small class="muted">เป้าหมาย ≥ 80%</small></div><div class="quality-list">${[['ความครบถ้วนของข้อมูล',d.average||0],['ประวัติและการตรวจร่างกาย',Math.min(100,(d.average||0)+4)],['การลงนามผู้รับผิดชอบ',Math.max(0,(d.average||0)-7)],['แผนรักษาต่อเนื่อง',Math.max(0,(d.average||0)-3)]].map(x=>`<div class="quality-row"><div><span>${x[0]}</span><b>${x[1]}%</b></div><div class="bar"><i style="width:${x[1]}%"></i></div></div>`).join('')}</div></div></div><div class="card" style="margin-top:18px"><div class="card-head"><h3>ทางลัด</h3></div><div class="card-body quick-actions"><button class="quick" onclick="navigate('records')"><span>⌕</span><b>ค้นเวชระเบียน</b><small>ค้นด้วย HN, VN, AN หรือชื่อ</small></button><button class="quick" onclick="navigate('audits')"><span>✓</span><b>ติดตามงาน Audit</b><small>ฉบับร่างและรายการที่เสร็จแล้ว</small></button><button class="quick" onclick="navigate('reports')"><span>⌁</span><b>ดูรายงานคุณภาพ</b><small>แนวโน้มและจุดที่ควรปรับปรุง</small></button></div></div>`}
function recent(x){return `<div class="recent-row"><span class="type-badge ${x.record_type==='IPD'?'ipd':''}">${x.record_type}</span><div><b>${esc(x.patient_name||x.ref_no)}</b><small>${x.ref_no} • ${fmtDate(x.updated_at)}</small></div><div class="score-ring" style="--p:${x.percent}"><span>${x.percent}%</span></div></div>`}

function records(){const el=$('#page-records');el.innerHTML=`<div class="hero"><div><p class="eyebrow">HOSXP RECORD FINDER</p><h1>เลือกเวชระเบียนเพื่อประเมิน</h1><p>ข้อมูลที่เรียกดูเป็นแบบอ่านอย่างเดียว ไม่แก้ไขข้อมูลต้นทาง</p></div></div><div class="card filter-card"><div class="field"><label>HN / VN / AN / ชื่อผู้ป่วย<input id="searchQ" placeholder="พิมพ์คำค้น..."></label></div><div class="field"><label>ประเภท<select id="searchType"><option>ALL</option><option>OPD</option><option>IPD</option></select></label></div><div class="field"><label>ตั้งแต่วันที่<input id="fromDate" type="date"></label></div><div class="field"><label>ถึงวันที่<input id="toDate" type="date"></label></div><button id="searchBtn" class="primary">ค้นหา</button></div><div class="card" style="margin-top:16px"><div class="card-head"><h3>เวชระเบียนล่าสุด</h3><small id="resultCount" class="muted"></small></div><div id="caseResults" class="table-wrap"><div class="empty">กำลังเรียกข้อมูล...</div></div></div>`;$('#searchBtn').onclick=searchCases;$('#searchQ').onkeydown=e=>{if(e.key==='Enter')searchCases()};searchCases()}
async function searchCases(){const q=encodeURIComponent($('#searchQ')?.value||''),t=$('#searchType')?.value||'ALL',f=$('#fromDate')?.value||'',to=$('#toDate')?.value||'';$('#caseResults').innerHTML='<div class="empty">กำลังเรียกข้อมูลจาก HOSxP...</div>';try{const d=await api(`/api/cases?q=${q}&record_type=${t}&date_from=${f}&date_to=${to}`);$('#resultCount').textContent=`${d.count} รายการ • ${d.source==='demo'?'ข้อมูลสาธิต':'HOSxP'}`;$('#caseResults').innerHTML=d.items.length?`<table class="data-table"><thead><tr><th>ประเภท</th><th>ผู้ป่วย</th><th>วันที่</th><th>หน่วยบริการ</th><th>แพทย์</th><th></th></tr></thead><tbody>${d.items.map(x=>`<tr><td><span class="type-badge ${x.type==='IPD'?'ipd':''}">${x.type}</span></td><td class="patient-cell"><b>${esc(x.patient_name)}</b><small>HN ${esc(x.hn)} • ${x.type==='IPD'?'AN':'VN'} ${esc(x.ref_no)}</small></td><td>${fmtDate(x.visit_date)}</td><td>${esc(x.department)}</td><td>${esc(x.doctor_name)}</td><td><button class="link-btn open-case" data-type="${x.type}" data-ref="${x.ref_no}">เปิดประเมิน →</button></td></tr>`).join('')}</tbody></table>`:'<div class="empty">ไม่พบเวชระเบียนตามเงื่อนไข</div>';$$('.open-case').forEach(b=>b.onclick=()=>openCase(b.dataset.type,b.dataset.ref))}catch(e){$('#caseResults').innerHTML=`<div class="empty">${esc(e.message)}</div>`}}
async function openCase(type,ref){toast('กำลังเตรียมเวชระเบียน...');try{state.case=await api(`/api/cases/${type}/${ref}`);state.auditId=null;state.scores={};state.annotations=[];state.files=[];renderWorkspace();navigate('workspace')}catch(e){toast(e.message)}}

function renderWorkspace(){const c=state.case,criteria=c.type==='IPD'?IPD:OPD;criteria.forEach((g,gi)=>{if(!state.scores[gi])state.scores[gi]={name:g[0],items:g[1].map(text=>({text,state:''}))}});$('#page-workspace').innerHTML=`<div class="workspace"><section class="record-pane"><div class="patient-header"><span class="avatar">${c.type}</span><div><h2>${esc(c.patient_name)}</h2><p>HN ${esc(c.hn)} • ${c.type==='IPD'?'AN':'VN'} ${esc(c.ref_no)}</p></div><span style="margin-left:auto" class="badge green">● ข้อมูลจาก ${state.demo?'ชุดสาธิต':'HOSxP'}</span></div><div class="patient-meta"><div><small>${c.type==='IPD'?'วันที่ Admit':'วันที่รับบริการ'}</small><b>${fmtDate(c.visit_date)}</b></div><div><small>อายุ / เพศ</small><b>${esc(c.age)} / ${c.sex==='1'||c.sex==='ชาย'?'ชาย':'หญิง'}</b></div><div><small>หน่วยบริการ</small><b>${esc(c.department)}</b></div><div><small>แพทย์</small><b>${esc(c.doctor_name)}</b></div></div>${recordSection('อาการสำคัญ','Chief complaint',c.chief_complaint)}${recordSection('ประวัติปัจจุบัน','Present illness',c.present_illness)}${vitals(c.vitals)}${diagnoses(c.diagnoses)}${c.type==='IPD'?ipdDaily(c):`${treatments(c.medications)}${c.labs?.length?dailyLabs(c.labs):''}`}${annotationsHtml()}</section><section class="audit-pane"><div class="audit-head"><div class="audit-head-row"><div><p class="eyebrow" style="margin:0">${c.type} AUDIT</p><h2>แบบประเมินคุณภาพ</h2></div><div class="live-score"><span class="muted">คะแนน</span><strong id="liveScore">0/0</strong></div></div><div class="progress-track"><i id="scoreProgress" style="width:0"></i></div></div><div class="audit-body"><div class="finding-box"><b style="font-size:12px">ผลการทบทวนโดยรวม</b><label><input type="radio" name="finding" value="inadequate"> ข้อมูลไม่เพียงพอสำหรับการทบทวน</label><label><input type="radio" name="finding" value="no_issue" checked> ไม่พบประเด็นสำคัญของเวชระเบียน</label><label><input type="radio" name="finding" value="issue"> พบประเด็นที่ต้องทบทวน</label><textarea id="issueText" placeholder="สรุปประเด็นหรือข้อเสนอแนะ..."></textarea></div>${criteria.map((g,i)=>accordion(g,i)).join('')}<div class="attach-box"><label for="fileInput"><b>＋ แนบหลักฐานประกอบ</b><br><span class="muted">PDF, JPG, PNG, WEBP • ไม่เกิน 10 MB</span></label><input id="fileInput" type="file" accept=".pdf,.jpg,.jpeg,.png,.webp"><div id="fileList">${filesHtml()}</div></div></div><div class="audit-footer"><button class="secondary" onclick="navigate('records')">← กลับ</button><button id="saveDraft" class="secondary">บันทึกร่าง</button><button id="saveComplete" class="primary">เสร็จสิ้นการประเมิน</button></div></section></div>`;wireWorkspace();updateScore()}
function recordSection(th,en,text){return `<div class="record-section"><h3>${th}<button class="highlight-btn" data-section="${en}">＋ ไฮไลต์ข้อความที่เลือก</button></h3><div class="content record-text" data-section="${en}">${esc(text||'ไม่มีข้อมูล')}</div></div>`}
function vitals(v={}){return `<div class="record-section"><h3>สัญญาณชีพ <span class="muted">Vital signs</span></h3><table class="mini-table"><tr><th>Temp</th><th>Pulse</th><th>RR</th><th>BP</th><th>Weight</th><th>Height</th></tr><tr><td>${v.temperature||'-'} °C</td><td>${v.pulse||'-'}</td><td>${v.respiration||'-'}</td><td>${v.bp||'-'}</td><td>${v.weight||'-'} kg</td><td>${v.height||'-'} cm</td></tr></table></div>`}
function diagnoses(a=[]){return `<div class="record-section"><h3>การวินิจฉัย <span class="muted">Diagnosis</span></h3><table class="mini-table"><tr><th>ICD-10</th><th>รายละเอียด</th><th>ประเภท</th></tr>${a.map(x=>`<tr><td><b>${esc(x.code)}</b></td><td>${esc(x.name)}</td><td>${esc(x.type)}</td></tr>`).join('')||'<tr><td colspan="3">ไม่มีข้อมูล</td></tr>'}</table></div>`}
const TREATMENT_TYPES={1:['ยา','Medication'],2:['บริการและหัตถการ','Service / Procedure'],3:['คำสั่งตรวจทางห้องปฏิบัติการ','Laboratory order'],4:['รังสีและภาพวินิจฉัย','Radiology'],5:['ห้องและเตียง','Room / Bed'],6:['อาหาร','Diet'],7:['เวชภัณฑ์','Medical supply'],8:['รายการอื่นๆ','Other']};
function treatmentType(x){const text=`${x.income_name||''} ${x.name||''}`.toLocaleLowerCase('th-TH'),usage=String(x.drugusage||'').trim();if(x.catalog_kind==='drug'||usage||Number.parseInt(x.item_type,10)===1)return 1;if(/lab|laboratory|ห้องปฏิบัติการ|ชันสูตร|ตรวจเลือด|ตรวจปัสสาวะ|ตรวจอุจจาระ|\b(cbc|bun|creatinine|electrolyte|glucose|hba1c|wbc|rbc|platelet|hematocrit|haemoglobin|hemoglobin|sodium|potassium|chloride|bilirubin|albumin|sgot|sgpt|ast|alt|alp|tsh|ft3|ft4|inr|ptt|pcr|culture|antigen|antibody|urinalysis)\b/i.test(text))return 3;if(/x[ -]?ray|radiology|รังสี|เอกซเรย์|อัลตราซาวด์|ultrasound|\b(ct|mri)\b/i.test(text))return 4;if(/ห้องพิเศษ|ค่าห้อง|ค่าเตียง|ห้องและเตียง|room|bed/i.test(text))return 5;if(/อาหาร|โภชน|diet|food/i.test(text))return 6;if(/เวชภัณฑ์|วัสดุ|อุปกรณ์|medical supply|supply/i.test(text))return 7;if(/บริการ|หัตถการ|ผ่าตัด|พยาบาล|ตรวจรักษา|service|procedure|operation/i.test(text))return 2;return 8}
function treatmentTable(a=[],type=8){const [title,en]=TREATMENT_TYPES[type];return `<div class="record-section"><h3>${title} <span class="muted">${en}</span></h3><table class="mini-table"><tr><th>รายการ</th><th>วิธีใช้/รายละเอียด</th><th>จำนวน</th></tr>${a.map(x=>{const detail=x.usage&&x.usage!=='-'?x.usage:(x.income_name||'-');return `<tr><td><b>${esc(x.name||x.icode)}</b>${x.name&&String(x.name)!==String(x.icode)?`<small>รหัส ${esc(x.icode)}${x.income_name?` • ${esc(x.income_name)}`:''}</small>`:''}</td><td>${esc(detail)}</td><td>${esc(x.qty??'-')}</td></tr>`}).join('')}</table></div>`}
function treatments(a=[]){if(!a.length)return '<div class="record-section"><h3>ยาและการรักษา</h3><div class="empty compact">ไม่มีข้อมูล</div></div>';const groups=new Map;for(const item of a){const type=treatmentType(item);if(!groups.has(type))groups.set(type,[]);groups.get(type).push(item)}return [...groups.entries()].sort((a,b)=>a[0]-b[0]).map(([type,items])=>treatmentTable(items,type)).join('')}
function dailyLabs(a=[]){return `<div class="record-section"><h3>ผลตรวจทางห้องปฏิบัติการ <span class="muted">Laboratory</span></h3><table class="mini-table"><tr><th>รายการ</th><th>ผลตรวจ</th><th>ค่าปกติ</th><th>สถานะ</th></tr>${a.map(x=>`<tr><td>${esc(x.name||'-')}</td><td>${esc(x.result||'-')}</td><td>${esc(x.normal_value||'-')}</td><td>${x.abnormal_result==='Y'?'<span class="badge red">ผิดปกติ</span>':x.abnormal_result?'<span class="badge green">ปกติ</span>':'-'}</td></tr>`).join('')}</table></div>`}
function dailyTimeline(a=[]){return `<div class="record-section"><h3>เหตุการณ์และการย้ายหอผู้ป่วย <span class="muted">Timeline</span></h3><table class="mini-table"><tr><th>เวลา</th><th>เหตุการณ์</th><th>รายละเอียด</th></tr>${a.map(x=>`<tr><td>${esc(x.event_time||'-')}</td><td>${esc(x.title||'-')}</td><td>${esc(x.detail||'-')}</td></tr>`).join('')}</table></div>`}
function ipdDaily(c){const dateOf=x=>String(x?.event_date||x?.date||'').slice(0,10),medications=c.medications||[],labs=c.labs||[],timeline=c.timeline||[],dates=[...new Set([...medications.map(dateOf),...labs.map(dateOf),...timeline.map(dateOf),String(c.admit?.regdate||'').slice(0,10),String(c.admit?.dchdate||'').slice(0,10)].filter(Boolean))].sort();if(!dates.length)return treatments(medications);return `<div class="record-section ipd-daily"><h3>ข้อมูลการรักษารายวัน <span class="muted">Daily inpatient record</span></h3><div class="ipd-date-tabs" role="tablist" aria-label="วันที่รักษาผู้ป่วยใน">${dates.map((date,i)=>{const count=medications.filter(x=>dateOf(x)===date).length+labs.filter(x=>dateOf(x)===date).length+timeline.filter(x=>dateOf(x)===date).length;return `<button class="ipd-date-tab ${i===0?'active':''}" data-day="${i}" role="tab" aria-selected="${i===0}"><b>วันที่ ${i+1}</b><small>${fmtDate(date)} • ${count} รายการ</small></button>`}).join('')}</div><div class="ipd-day-panels">${dates.map((date,i)=>{const dayMeds=medications.filter(x=>dateOf(x)===date),dayLabs=labs.filter(x=>dateOf(x)===date),dayTimeline=timeline.filter(x=>dateOf(x)===date);return `<div class="ipd-day-panel ${i===0?'active':''}" data-day-panel="${i}"><div class="ipd-date-heading"><div><span class="type-badge ipd">${i+1}</span><div><b>วันที่รักษาลำดับที่ ${i+1}</b><small>${fmtDate(date)}</small></div></div></div>${dayMeds.length?treatments(dayMeds):''}${dayLabs.length?dailyLabs(dayLabs):''}${dayTimeline.length?dailyTimeline(dayTimeline):''}${!dayMeds.length&&!dayLabs.length&&!dayTimeline.length?'<div class="empty compact">ไม่มีรายการบันทึกในวันนี้</div>':''}</div>`}).join('')}</div></div>`}
function annotationsHtml(){return `<div class="record-section"><h3>ไฮไลต์และข้อสังเกต <span class="muted">${state.annotations.length} รายการ</span></h3><div class="content" id="annotationList">${state.annotations.length?state.annotations.map((x,i)=>`<div class="annotation"><b>${esc(x.section)} • <button class="link-btn del-note" data-i="${i}">ลบ</button></b>“${esc(x.quote)}”<br>${esc(x.comment)}</div>`).join(''):'<span class="muted">เลือกข้อความในเวชระเบียน แล้วกด “ไฮไลต์ข้อความที่เลือก”</span>'}</div></div>`}
function accordion(g,gi){const s=state.scores[gi];let done=s.items.filter(x=>x.state).length;return `<div class="accordion"><button class="acc-head" data-acc="${gi}"><span class="num">${gi+1}</span><b>${g[0]}</b><small>${done}/${s.items.length}</small><span>⌄</span></button><div class="acc-body" id="acc-${gi}" style="display:${gi<2?'block':'none'}">${s.items.map((x,ii)=>`<div class="criterion"><span class="idx">${ii+1}.</span><p>${x.text}</p><div class="score-buttons">${['1','0','NA','M'].map(v=>`<button data-group="${gi}" data-item="${ii}" data-score="${v}" class="${x.state===v?'sel':''}" title="${v==='1'?'ครบถ้วน':v==='0'?'ไม่ครบ':v==='NA'?'ไม่เกี่ยวข้อง':'เอกสารขาด'}">${v}</button>`).join('')}</div></div>`).join('')}</div></div>`}
function filesHtml(){return state.files.map(f=>`<div class="file-chip"><span>${esc(f.original_name||f.name)}</span><small>${Math.round(f.size/1024)} KB</small></div>`).join('')}
function wireWorkspace(){$$('.ipd-date-tab').forEach(tab=>tab.onclick=()=>{$$('.ipd-date-tab').forEach(x=>{const active=x===tab;x.classList.toggle('active',active);x.setAttribute('aria-selected',String(active))});$$('.ipd-day-panel').forEach(x=>x.classList.toggle('active',x.dataset.dayPanel===tab.dataset.day))});$$('.acc-head').forEach(b=>b.onclick=()=>{const x=$(`#acc-${b.dataset.acc}`);x.style.display=x.style.display==='none'?'block':'none'});$$('.score-buttons button').forEach(b=>b.onclick=()=>{state.scores[b.dataset.group].items[b.dataset.item].state=b.dataset.score;const group=b.parentElement;[...group.children].forEach(x=>x.classList.toggle('sel',x===b));updateScore()});$$('.highlight-btn').forEach(b=>b.onclick=()=>addHighlight(b.dataset.section));$$('.del-note').forEach(b=>b.onclick=()=>{state.annotations.splice(+b.dataset.i,1);renderWorkspace()});$('#saveDraft').onclick=()=>saveAudit('draft');$('#saveComplete').onclick=()=>saveAudit('completed');$('#fileInput').onchange=uploadFile;if(state.role==='viewer'){$$('.score-buttons button,.highlight-btn,#saveDraft,#saveComplete,#fileInput').forEach(x=>x.disabled=true);toast('บัญชีผู้ดูรายงานเปิดดูได้อย่างเดียว')}}
function updateScore(){const items=Object.values(state.scores).flatMap(x=>x.items),yes=items.filter(x=>x.state==='1').length,max=items.filter(x=>['1','0'].includes(x.state)).length,p=max?Math.round(yes*100/max):0;$('#liveScore').textContent=`${yes}/${max}`;$('#scoreProgress').style.width=`${p}%`}
function addHighlight(section){const sel=window.getSelection(),quote=sel.toString().trim();if(!quote){toast('กรุณาลากเลือกข้อความก่อน');return}const comment=prompt('เพิ่มข้อสังเกตสำหรับข้อความนี้');if(comment===null)return;state.annotations.push({section,quote:quote.slice(0,500),comment:comment.slice(0,500),created_at:new Date().toISOString()});renderWorkspace();toast('เพิ่มไฮไลต์แล้ว')}
async function saveAudit(status){const c=state.case,body={id:state.auditId,record_type:c.type,ref_no:c.ref_no,hn:c.hn,patient_name:c.patient_name,department:c.department,status,finding:$('input[name=finding]:checked')?.value||'no_issue',issue_text:$('#issueText').value,scores:state.scores,annotations:state.annotations};try{const d=await api('/api/audits',{method:'POST',body:JSON.stringify(body)});state.auditId=d.id;toast(status==='completed'?`เสร็จสิ้น • ${d.percent}%`:'บันทึกร่างแล้ว');if(status==='completed')setTimeout(()=>navigate('audits'),700)}catch(e){toast(e.message)}}
async function uploadFile(e){if(!e.target.files[0])return;if(!state.auditId){await saveAudit('draft')}const fd=new FormData;fd.append('file',e.target.files[0]);try{const f=await api(`/api/audits/${state.auditId}/attachments`,{method:'POST',body:fd});state.files.push(f);$('#fileList').innerHTML=filesHtml();toast('แนบไฟล์แล้ว')}catch(err){toast(err.message)}}

async function audits(){const d=await api('/api/audits');state.audits=d.items;$('#page-audits').innerHTML=`<div class="hero"><div><p class="eyebrow">AUDIT WORKLIST</p><h1>รายการประเมินทั้งหมด</h1><p>ติดตามฉบับร่าง งานที่เสร็จสิ้น และคะแนนคุณภาพ</p></div><button class="primary" onclick="navigate('records')">＋ เริ่มประเมินใหม่</button></div><div class="card"><div class="table-wrap">${d.items.length?`<table class="data-table"><thead><tr><th>ประเภท</th><th>ผู้ป่วย</th><th>หน่วยงาน</th><th>ผู้ตรวจ</th><th>สถานะ</th><th>คะแนน</th><th>อัปเดต</th></tr></thead><tbody>${d.items.map(x=>`<tr><td><span class="type-badge ${x.record_type==='IPD'?'ipd':''}">${x.record_type}</span></td><td class="patient-cell"><b>${esc(x.patient_name)}</b><small>HN ${x.hn} • ${x.ref_no}</small></td><td>${esc(x.department)}</td><td>${esc(x.auditor)}</td><td>${badge(x.status==='completed'?'เสร็จสิ้น':'ฉบับร่าง')}</td><td><b style="color:${x.percent>=80?'var(--green)':'var(--red)'}">${x.total_score}/${x.max_score} (${x.percent}%)</b></td><td>${fmtDate(x.updated_at)}</td></tr>`).join('')}</tbody></table>`:'<div class="empty">ยังไม่มีรายการประเมิน</div>'}</div></div>`}
async function reports(){const d=await api('/api/audits?limit=500'),done=d.items.filter(x=>x.status==='completed'),avg=done.length?Math.round(done.reduce((s,x)=>s+x.percent,0)/done.length):0,months=['มี.ค.','เม.ย.','พ.ค.','มิ.ย.','ก.ค.','ส.ค.'];$('#page-reports').innerHTML=`<div class="hero"><div><p class="eyebrow">QUALITY ANALYTICS</p><h1>รายงานคุณภาพเวชระเบียน</h1><p>สรุปผลเพื่อใช้ในกระบวนการ CQI และส่งกลับหน่วยงาน</p></div><button class="secondary" onclick="window.print()">⇩ พิมพ์ / บันทึก PDF</button></div><div class="stats"><div class="stat"><small>คะแนนเฉลี่ยรวม</small><strong>${avg}%</strong><span class="delta">เป้าหมาย ≥ 80%</span></div><div class="stat"><small>ผ่านเกณฑ์</small><strong>${done.filter(x=>x.percent>=80).length}</strong><span class="delta">จาก ${done.length} รายการเสร็จสิ้น</span></div><div class="stat"><small>OPD Audited</small><strong>${done.filter(x=>x.record_type==='OPD').length}</strong></div><div class="stat"><small>IPD Audited</small><strong>${done.filter(x=>x.record_type==='IPD').length}</strong></div></div><div class="grid-main"><div class="card"><div class="card-head"><h3>แนวโน้มคะแนนคุณภาพ</h3><small class="muted">6 เดือนล่าสุด</small></div><div class="chart">${months.map((m,i)=>{let v=Math.max(3,avg-(5-i)*2);return `<div class="chart-col"><b>${v}%</b><i style="height:${v}%"></i><small>${m}</small></div>`}).join('')}</div></div><div class="card"><div class="card-head"><h3>ข้อเสนอแนะเชิงระบบ</h3></div><div class="card-body"><p><span class="badge amber">โอกาสพัฒนา</span></p><b>เน้นความครบถ้วนของลายมือชื่อและเวลา</b><p class="muted">ควรติดตามเกณฑ์ที่ได้ 0 บ่อย แยกตามหน่วยงาน และทบทวนซ้ำหลัง CQI</p><hr style="border:0;border-top:1px solid var(--line)"><b>Sampling ที่แนะนำ</b><p class="muted">สุ่มแบบ stratified แยก OPD/IPD หน่วยงาน และระดับความเสี่ยง เพื่อลด selection bias</p></div></div></div>`}
const roleText={admin:'ผู้ดูแลระบบ',auditor:'ผู้ตรวจประเมิน',viewer:'ผู้ดูรายงาน'};
async function members(){const d=await api('/api/users');$('#page-members').innerHTML=`<div class="hero"><div><p class="eyebrow">MEMBER ACCESS</p><h1>สมาชิกและสิทธิ์การใช้งาน</h1><p>เพิ่มผู้ใช้งาน กำหนดบทบาท รีเซ็ตรหัสผ่าน หรือระงับบัญชี</p></div></div><form id="memberForm" class="card filter-card" style="grid-template-columns:1fr 1.4fr 1fr 1fr auto"><div class="field"><label>ชื่อผู้ใช้<input id="newUsername" required placeholder="เช่น auditor01"></label></div><div class="field"><label>ชื่อที่แสดง<input id="newDisplayName" required placeholder="ชื่อ–สกุล หรือหน่วยงาน"></label></div><div class="field"><label>บทบาท<select id="newRole"><option value="auditor">ผู้ตรวจประเมิน</option><option value="viewer">ผู้ดูรายงาน</option><option value="admin">ผู้ดูแลระบบ</option></select></label></div><div class="field"><label>รหัสผ่านเริ่มต้น<input id="newPassword" type="password" minlength="6" required></label></div><button class="primary" type="submit">＋ เพิ่มสมาชิก</button></form><div class="card" style="margin-top:16px"><div class="card-head"><h3>บัญชีทั้งหมด</h3><small class="muted">${d.items.length} บัญชี</small></div><div class="table-wrap"><table class="data-table"><thead><tr><th>สมาชิก</th><th>บทบาท</th><th>สถานะ</th><th>เข้าใช้ล่าสุด</th><th>สร้างเมื่อ</th><th></th></tr></thead><tbody>${d.items.map(x=>`<tr><td class="patient-cell"><b>${esc(x.display_name)}</b><small>@${esc(x.username)}</small></td><td><select class="role-select" data-id="${x.id}" ${x.username===state.user?'disabled':''}>${Object.entries(roleText).map(([v,t])=>`<option value="${v}" ${x.role===v?'selected':''}>${t}</option>`).join('')}</select></td><td>${x.active?'<span class="badge green">● ใช้งาน</span>':'<span class="badge red">● ระงับ</span>'}</td><td>${x.last_login?new Date(x.last_login).toLocaleString('th-TH'):'ยังไม่เคยเข้า'}</td><td>${fmtDate(x.created_at)}</td><td><button class="link-btn reset-user" data-id="${x.id}" data-name="${esc(x.username)}">รีเซ็ตรหัส</button> ${x.username!==state.user?`<button class="link-btn toggle-user" data-id="${x.id}" data-active="${x.active?0:1}">${x.active?'ระงับ':'เปิดใช้'}</button>`:''}</td></tr>`).join('')}</tbody></table></div></div>`;$('#memberForm').onsubmit=createMember;$$('.role-select').forEach(x=>x.onchange=()=>updateMember(x.dataset.id,{role:x.value}));$$('.toggle-user').forEach(x=>x.onclick=()=>updateMember(x.dataset.id,{active:x.dataset.active==='1'}));$$('.reset-user').forEach(x=>x.onclick=()=>resetMember(x.dataset.id,x.dataset.name))}
async function createMember(e){e.preventDefault();try{await api('/api/users',{method:'POST',body:JSON.stringify({username:$('#newUsername').value,display_name:$('#newDisplayName').value,role:$('#newRole').value,password:$('#newPassword').value})});toast('เพิ่มสมาชิกแล้ว');members()}catch(err){toast(err.message)}}
async function updateMember(id,changes){try{await api(`/api/users/${id}`,{method:'PATCH',body:JSON.stringify(changes)});toast('อัปเดตสิทธิ์แล้ว');members()}catch(err){toast(err.message);members()}}
async function resetMember(id,name){const password=prompt(`ตั้งรหัสผ่านใหม่สำหรับ ${name} (อย่างน้อย 6 ตัวอักษร)`);if(password===null)return;try{await api(`/api/users/${id}`,{method:'PATCH',body:JSON.stringify({password})});toast('เปลี่ยนรหัสผ่านแล้ว')}catch(err){toast(err.message)}}
async function activity(){const d=await api('/api/activity');$('#page-activity').innerHTML=`<div class="hero"><div><p class="eyebrow">SECURITY & TRACEABILITY</p><h1>ประวัติการใช้งาน</h1><p>ตรวจสอบการเข้าถึงเวชระเบียนและการเปลี่ยนแปลงผลประเมิน</p></div></div><div class="card"><div class="table-wrap"><table class="data-table"><thead><tr><th>วันเวลา</th><th>ผู้ใช้งาน</th><th>กิจกรรม</th><th>รายการ</th><th>รายละเอียด</th></tr></thead><tbody>${d.items.map(x=>`<tr><td>${new Date(x.created_at).toLocaleString('th-TH')}</td><td>${esc(x.actor)}</td><td><span class="badge">${esc(x.action)}</span></td><td>${esc(x.target)}</td><td>${esc(x.detail)}</td></tr>`).join('')}</tbody></table></div></div>`}
boot().catch(()=>showLogin());
